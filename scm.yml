---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany # ReadOnlyMany not supported
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1 
kind: Deployment
metadata:
  labels:
    run: sdorra/scm-manager
  name: sdorra/scm-manager-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      run: sdorra/scm-manager
  template:
    metadata:
      labels:
        run: sdorra/scm-manager
    spec:
      volumes:
      - name: www
        persistentVolumeClaim:
          claimName: test-pvc
      containers:
      - image: sdorra/scm-manager
        name: sdorra/scm-manager
        volumeMounts:
        - name: www
          mountPath: /usr/share/sdorra/scm-manager/html
        ports:
          - name: http
            containerPort: 80 
            
---
apiVersion: v1
kind: Service
metadata:
  name: sdorra/scm-manager-service
spec:
  selector:
    run: sdorra/scm-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdorra/scm-manager-ingress
spec:
  rules:
  - host: sdorra/scm-manager.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sdorra/scm-manager-service
            port:
              number: 80

